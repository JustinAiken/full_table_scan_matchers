source 'https://rubygems.org'

# Specify your gem's dependencies in full_table_scan_matchers.gemspec
gemspec

if ENV['RAILS_VERSION']
  gem 'activesupport', "~> #{ENV['RAILS_VERSION']}"
  gem 'activerecord',  "~> #{ENV['RAILS_VERSION']}"
end
