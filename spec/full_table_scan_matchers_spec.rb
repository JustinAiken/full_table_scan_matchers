require 'spec_helper'

describe FullTableScanMatchers do
  it 'has a version number' do
    expect(FullTableScanMatchers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
